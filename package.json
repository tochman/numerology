{
  "name": "numerology",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "bdd-lazy-var": "^2.6.1",
    "chai": "^4.3.4",
    "mocha": "^9.1.3"
  },
  "scripts": {
    "test": "mocha --file spec/specHelper.js -u bdd-lazy-var/global --recursive --exit spec/**/*.spec.js"
  }
}
